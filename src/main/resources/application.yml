spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_factory?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  # 排除 Spring AI MCP SSE 自动配置——GitNexus 使用 Streamable HTTP 协议,
  # 由 GitNexusMcpClientConfiguration 提供自定义 McpSyncClient bean。
  autoconfigure:
    exclude:
      - org.springframework.ai.mcp.client.autoconfigure.McpClientAutoConfiguration
      - org.springframework.ai.mcp.client.autoconfigure.McpToolCallbackAutoConfiguration
      - org.springframework.ai.mcp.client.autoconfigure.StdioTransportAutoConfiguration
      - org.springframework.ai.mcp.client.autoconfigure.SseHttpClientTransportAutoConfiguration
      - org.springframework.ai.mcp.client.autoconfigure.SseWebFluxTransportAutoConfiguration
  # Spring AI OpenAI 配置
  ai:
    openai:
      # Spring AI 会自动追加 /v1/chat/completions，base-url 不要带 /v1
      base-url: ${LLM_BASE_URL:http://localhost:11434}
      api-key: ${LLM_API_KEY:dummy}
      chat:
        options:
          model: ${LLM_MODEL:qwen2.5:14b}
          temperature: 0.2

gitnexus:
  base-url: http://localhost:4747
  mcp:
    url: ${GITNEXUS_MCP_URL:http://localhost:4747/api/mcp}
  default-repo: GitNexus

factory:
  clients:
    real:
      enabled: true
